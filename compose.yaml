services:
  devmarkt:
    container_name: bivieh_devmarkt
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
        - ./:/var/www/
        # - ./impressum.html:/var/www/public/impressum.html
    ports:
        - "8080:80"
    env_file: .env
